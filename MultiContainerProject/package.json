{
  "name": "multicontainerproject",
  "version": "1.0.0",
  "description": "MultiContainer Setup for Node and Postgres",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "db:migration":"npx prisma migrate dev --create-only",
    "db:migrate":"npx prisma migrate dev",
    "prisma:generate":"npx prisma generate",
    "seed":"node ./prisma/seed.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "prisma": "^6.3.1"
  }
}
